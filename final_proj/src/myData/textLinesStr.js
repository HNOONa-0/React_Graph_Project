// changes
// new, use textLinesStr as refrence in textBox refer
import { setLines } from "../myUtils/setLines";
import { maxLines } from "./limits";

const textLinesStr = setLines(maxLines + 100);
export default textLinesStr;
